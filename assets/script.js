//create the list of 'computer choices'
//ask  the user for the 'userChoice'
//Randomly generate the computers choice
	
	//randomly generate an index (with array?)
	// use the indexc to select from 'computerchoices'

//determine the result of the game, by comparing 'userChoice'against 'computerchoice' to determine a 'win', 'loss', or 'tie'